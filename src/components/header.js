import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1>Recipe Finder</h1>
      <nav>
        {/* Add navigation links here */}
      </nav>
    </header>
  );
};

export default Header;
